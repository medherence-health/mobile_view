import 'package:flutter/material.dart';

class WalletPinWidget extends StatelessWidget {
  const WalletPinWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog.adaptive();
  }
}
